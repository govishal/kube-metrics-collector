apiVersion: v1
kind: Pod
metadata:
  name: my-first-pod
  labels:
    app: my-app
    environment: development
spec:
  containers:
  - name: my-container
    image: nginx:latest
    ports:
    - containerPort: 80